export interface IFilter {
  name: string;
  status: number;
  recipients: string;
  success: string;
  id: string;
}

export interface IJourney {
  name: string;
  status: number;
  recipients: string;
  success: string;
  id: string;
}
